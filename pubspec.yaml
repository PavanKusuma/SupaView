name: supaview
description: Speech analysis app for interview preparation. Analyze pauses, filler words, confidence and more.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: ^0.11.0+2
  speech_to_text: ^7.0.0
  provider: ^6.1.1
  fl_chart: ^0.69.0
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  intl: ^0.19.0
  permission_handler: ^11.1.0
  uuid: ^4.2.1
  video_player: ^2.8.2
  file_picker: ^6.1.1
  share_plus: ^7.2.1
  percent_indicator: ^4.2.3
  google_fonts: ^6.1.0
  lottie: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
